
## 소셜 로그인

프로젝트에서 인증 기능을 담당할 사람이 필요했을 때, 나는 소셜 로그인을 맡아보고 싶다고 말했다. 
구현을 시작하면서 어떤 인증 방식을 선택하느냐에 따라 필요한 보안 검증과 회원·소셜 계정의 데이터 구조가 크게 달라진다는 것을 알게 되었다.

다음 네 가지 방안을 놓고 고민했다.
- OIDC + Spring Security
- OIDC를 직접 구현하고 Spring Security를 사용하지 않는 방식
- OAuth + Spring Security
- OAuth와 보안 처리를 모두 직접 구현하는 방식


어떤 방식이 정답인지보다, 세 가지 소셜 제공자를 연결하면서 팀원이 계속 유지보수할 수 있는지를 기준으로 비교했다. 고민 끝에 OIDC와 Spring Security를 함께 사용하는 방식을 선택했다. 직접 구현해야 하는 인증 로직을 줄이면서도 여러 제공자의 사용자를 일관된 방식으로 연결할 수 있다고 판단했기 때문이다.


## 정상적으로 구현된 줄 알았던 로그인
구현 후 카카오 로그인 버튼을 눌렀을 때 바로 /applicants 화면으로 이동했다. 처음에는 소셜 로그인이 정상적으로 완료된 줄 알았지만, 시크릿 창에서 다시 테스트해보니 실제 카카오 로그인 화면을 거치지 않고 있었다.
원인은 프론트엔드의 MSW가 mock 세션을 만들어주고 있었기 때문이다. 실제 소셜 로그인과 mock 로그인이 겉으로는 같은 결과를 보여주기 때문에, 두 실행 모드를 분리하지 않으면 구현이 완료된 것처럼 착각할 수 있었다. 이후 실제 연동을 확인할 때는 MSW와 로컬 mock 로그인을 끄고 외부 제공자 화면에서 직접 로그인했다.

## 레벨4에서는
이번 작업을 통해 기능을 동작시키는 것과 그 코드의 구조와 흐름을 충분히 이해하는 것은 다르다는 것을 느꼈다. 눈앞의 기능을 완성하는 데 집중하면서 설계 의도와 각 코드의 책임을 깊이 정리하지 못한 부분이 기술부채로 남았다.
어쩌면 시간은 있었을지도 모른다. 그저 좀 나태해졌던게 아닌가 싶다.
레벨 4에서는 맡은 기능에 대해서 다른 팀원에게 왜 이렇게 구현했는지 설명하고 함께 수정할 수 있는 상태로 만드는 것을 목표로 하면서도, 타이트하게 일정을 진행하고 싶다.
